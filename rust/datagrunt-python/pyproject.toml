[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "datagrunt-rs"
version = "0.1.0"
description = "Prototype Rust port of datagrunt csvcomponents (not for distribution)"
requires-python = ">=3.10"

[tool.maturin]
module-name = "datagrunt_rs"
